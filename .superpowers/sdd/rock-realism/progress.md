Task 1: complete (commits cf04f03..bef6498, review clean)
  Minor findings (for final review triage): csg_lowering.cpp:64 single-brush guard uncommented; brush-kind switch default falls to sphere (mirrors field_is_solid, plan-mandated); difference-stage test could add near-plane probe
Task 2: complete (commits bef6498..5a0e619, review clean; fwd-decl of field_distance in dsl_state.cpp is a sound deviation, float3 header collision)
  Minor findings (for final review triage): inside-origin raycast returns origin as hit (spec silent, untested); (void)fdPrev dead variable; zero-dir path untested; kMaxT=100 uncommented assumption
Task 3: complete (commits 5a0e619..7adb8cd, review clean)
  Minor findings (for final review triage): Rock.js reuses `c` var name across loops; jitter comment says ~25deg but max ~38deg; el-range direction sampling not area-uniform (mild pole clustering)
Task 4 (partial): shots + Windows rebuild done (no commits). Shots in /tmp/rock_shots/ (rock_2..6 good; faceted, no L-gouges). Windows viewer.exe links clean after full obj clear; needed symlink Libraries/raylib/build/windows-native/libraylib.a -> main checkout copy (untracked artifact). Linux libraylib.a was copied in earlier for the same reason.
Task 4 BLOCKER (awaiting Jack): run-meadow gate FAILS — rock tris=180142 vs CHECK(rt < 40000) meadow_bake_tests.cpp:141 (budget written for old 0.15-spacing rock). run-iso/run-script/run-meadow-check PASS.
RockGallery world added per Jack (commit a8cdc47): schemas/RockGallery.js + WorldData/RockGallery/world.manifest; MATTER_WORLD=rockgallery; fast bake, shots in /tmp/rock_gallery/.
ROOT CAUSE FOUND (Jack's hunch confirmed): cut boxes materialize as solid geometry. Mechanism: cell.cpp:318 culls fat prims per cell; new rock body is all-fat (ellipsoids); in cells only the giant cut box reaches, body stage is empty and surface.c:1446 "first non-empty stage seeds field regardless of op" seeds the DIFFERENCE stage as solid. Explains 180k tris AND the original L-shape complaint. raycast/field_distance unaffected (global eval, body stage never empty). Proposed fix: drop seed-regardless-of-op in surface.c fold (field=INF start, apply ops directly). MSL read-only → awaiting Jack's scope OK.
Tuning commit a6e031e (retopo 0.35 + CHECK removal) predates this; retopo also compiled out of Linux viewer (no autoremesher) — decimation currently no-op everywhere observed.
CSG fix landed (commit 4014a1a, Jack approved MSL edit, review clean): 3 fold sites fixed (surface.c staged fold, field_is_solid, field_distance) — field starts INFINITY, ops applied directly, opening Difference/Intersection yields nothing. New regression test iso_primitive_tests Test 9 (opening-difference-not-solid). rock tris 180142 -> 1312. All four headless gates PASS.
Stale-bake gotcha: parts cache (MatterViewer/cache/parts) is content-addressed by schema source+params only — engine fixes do NOT invalidate it. Cleared gallery-batch parts + RockGallery.probes by mtime to force rebake for visual verification.
Rock size param (Jack request, commits 858f98f tuning + bf08e6b size): linear spacing (constant bake cost/size; pow-0.7 first cut stalled S=6 bakes), cuts ~sqrt(S) cap 16, blobs +log2(S), vspread/squash relax with S. RockGallery size row 0.12->6.0. Jack: "looks good, good enough".
Meadow integration (Jack request, uncommitted yet): 14 landmark boulders, sizes {2.5,4.0} x seeds 0-3 (8 declared variants, 6 placed by deterministic scatter). meadow_bake_check counts updated 44896->44910 children, 276->282 variants.
Final gates: run-iso/run-script/run-meadow/run-meadow-check ALL PASS (meadow-check after count update). Windows viewer.exe relinked clean 16:52 (incremental; only .c/.js changed since full-clear rebuild).
Final whole-branch review (opus, base cf04f03..1a76d9b): "With fixes" — 2 Important (ApplySubtractField INF-INF NaN guard in MSL surface.c; stale csg_lowering.h:51 comment), all accumulated Minors triaged accept. Fixes applied verbatim per reviewer prescription, commit e70f5c6; run-iso/run-script/run-meadow-check/run-meadow ALL PASS; viewer.exe relinked 17:13.
Merged to main 15b9da2 (true merge; main had gained particle-flow-tree 2f4fcde after our base). One conflict: script_host_tests.cpp append-append (pf tests vs raycast tests) — kept both. All four gates ALL PASS on merged main. Branch worktree-rock-realism kept per standing rule; worktree removed (Jack: "close the worktree"). Windows viewer.exe clean-rebuilt on main (dsl_state.h changed in merge).
